name = "json"
version = "0.1.2"
description = "JSON parsing and encoding for Donna"
licences = ["MIT"]
repository = { type = "github", user = "donna-lang", repo = "json" }
links = [{ title = "Docs", href = "https://donna-lang.github.io/json/" }]

[dependencies]
donna = { git = "https://github.com/donna-lang/donna_stdlib", version = ">=0.3.0 and <1.0.0" }

[dev-dependencies]
unittest = { git = "https://github.com/donna-lang/unittest", version = ">=0.2.0 and <1.0.0" }
